// Operador condicional ternário

let age = 18 
console.log(
    age >= 18 // Condição
    ? "Você pode dirigir." // Se verdadeiro
    : "Você não pode dirigir." // Se falso
)

console.log(age < 18 ? "Você não vota." : "Voto obrigatório.")